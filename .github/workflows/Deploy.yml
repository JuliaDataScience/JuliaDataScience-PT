name: Deploy

on:
  push:
    branches:
      - gh-pages
  # TMP
  pull_request:
  workflow_dispatch:

jobs:
  steps:
    - uses: actions/checkout@v2
      with:
        persist-credentials: false
        ref: gh-pages

    - run: julia src/push-jds.jl

